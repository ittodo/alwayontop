# Tray Always On Top - Context Shortcuts

This companion extension sends only the active VS Code shortcut context to the
locally running Tray Always On Top application through a current-user Windows
named pipe. It does not use the network or transmit document contents.
