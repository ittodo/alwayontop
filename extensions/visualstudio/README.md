# Tray Always On Top - Visual Studio integration

This VSIX reads the current Visual Studio command bindings and sends only shortcut metadata to the local Tray Always On Top process. Document contents and file paths are never transmitted.

The extension targets the stable Visual Studio 17.x API and supports Visual Studio 2022 and Visual Studio 2026.
